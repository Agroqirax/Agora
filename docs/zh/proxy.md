# 网络代理

打开**设置 → 网络代理**，让 Agora 共享 HTTP 客户端的流量经过指定代理。

## 作用范围

代理可能覆盖提供商请求、模型同步、网络搜索、HTTP MCP、更新检查，以及用户明确提交的评分/崩溃报告。它不会透明代理：

- 直接 SSH 连接
- 设备端 llama.cpp 推理
- Alpine 沙盒进程的网络

可配置代理类型、主机、端口、可选用户名/密码和绕过规则。代理密码只有在导出时明确选择机密/API Key 类别才会写入归档。
