# PDF 导入

从聊天编辑器附加 PDF，可把所选页面渲染成图片并发送给支持图像输入的模型。

## 页面选择

Agora 初始选择前 5 页，之后异步渲染文档并允许选择文件中的任意页面；应用没有固定的 50 页上限。大文档或大量页面会消耗更多内存和时间。

所选页面图片会成为对话附件，并按提供商的图像输入协议发送。只导入你愿意交给该提供商的文档。模型不支持图像时，请选择兼容模型或其他转录方式。
