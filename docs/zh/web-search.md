# 网络搜索

打开**设置 → 网络搜索**选择后端和结果数。

支持 Brave、Kagi、Serper、Tavily、SearXNG 与 DuckDuckGo。多数服务需要凭据；SearXNG 需要实例 URL。DuckDuckGo 是默认的尽力而为选项，不需要 API Key。

网络搜索默认开启，结果数默认 5，可设置 1–10。

查询会直接发送给所选搜索服务。返回的摘要和链接可作为工具数据保存在对话中，并在代理循环中发送给聊天模型。配额、政策和可用性由外部服务决定。
