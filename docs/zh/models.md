# 模型

打开**设置 → 模型**管理 Agora 可使用的模型。

## 默认模型

选择全局默认模型；对话仍可通过聊天选择器选用其他已启用模型。

## 已获取模型

从已配置提供商同步模型、搜索结果，并用复选框启用或停用。同步能力和元数据由提供商决定。

## 自定义模型

当提供商未列出所需 ID 时，可添加、编辑、设置别名或删除自定义模型。每个模型归属一个提供商，Agora 据此解析协议、Base URL 和凭据。

停用或删除条目只影响 Agora 的选择列表，不会删除远程数据。GGUF 文件请在[本地模型](local-model.md)中管理。
