# 系统提示词

打开**设置 → 系统提示词**管理可复用提示词。

新提示词可从**空白**或**默认**开始。编辑器包含有序的**系统、用户前缀、用户后缀**片段，支持文本、变量、上下移动和预览。

当前变量包括 `{time}`、`{date}`、`{sent_time}`、`{sent_date}`、`{active_memory}`、`{model_id}`，在使用时解析。

提示词可编辑、复制、删除并设为全局默认。对话可继承默认项或选择其他已保存提示词。内置 Default 来自应用源码，并不存在旧文档所说的四类模板库。
