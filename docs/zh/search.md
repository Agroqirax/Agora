# 对话搜索

打开**设置 → 对话搜索**配置对历史对话的语义访问。

**访问历史对话**默认开启；关闭后，模型/工具路径不会检索旧对话。活跃记忆和已保存记忆有独立权限。

可配置：

- 4–32 个对话步骤的上下文范围
- 5–30 条返回结果
- 0–1 的相似度阈值，默认 0.5
- 嵌入提供商/模型

搜索索引保存在本地，但远程嵌入模型会收到待嵌入文本；相关结果也可能加入后续模型请求。详见[嵌入 / RAG](embedding.md)。
